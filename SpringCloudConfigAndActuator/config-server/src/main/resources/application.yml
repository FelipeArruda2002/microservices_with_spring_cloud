server:
  port: 8888
spring:
  application:
  name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/FelipeArruda2002/config-server
          search-paths: greeting-service
#  profiles:
#    active:
#      - native